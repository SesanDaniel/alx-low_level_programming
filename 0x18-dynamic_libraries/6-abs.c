#include "main.h"

/**
 * _abs - check description
 * @r: in integer input
 * Description: a function that computes the absolute value of an integer.
 * Return: Nothing
 */

int _abs(int r)
{
	if (r >= 0)
		return (r);
	else
		return (r * -1);
}
