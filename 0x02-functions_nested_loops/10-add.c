#include "main.h"

/**
 * add - check description
 * Description: a function that adds two integers and returns the result.
 * Return: Nothing
 */

int add(int, int)
{
	char a, b, num;
	 num = (a + b);

	_putchar(num);
}
